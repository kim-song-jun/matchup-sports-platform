---
"v1_api": patch
---

Persist Alpha Firebase Admin credentials through the protected runtime env and fail deployment before activation when the dedicated `teameet-alpha` service account is unavailable or invalid.
