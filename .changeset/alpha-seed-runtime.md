---
"v1_api": patch
---

alpha 배포가 실패하던 것을 고쳤습니다. 배포 마지막 단계의 시드 하나가 **컨테이너 안에 없는 파일**을 참조해 죽었고, 그 뒤 새 코드가 alpha 에 올라가지 못했습니다(서비스는 옛 버전으로 정상 동작). 원인은 시드 내용이 아니라 **실행 방식**이었습니다 — 원본 TypeScript 를 그대로 돌리면 앱 코드를 찾지 못하는데, 배포 이미지는 컴파일된 결과만 싣기 때문입니다. 이미 옆의 다른 명령들이 쓰던 방식(컴파일본 실행)으로 맞췄고, 앞으로 같은 실수가 들어오면 CI 가 막습니다.
