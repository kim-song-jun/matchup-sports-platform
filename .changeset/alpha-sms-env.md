---
'v1_api': patch
---

alpha 는 인증번호를 발송하지 않고 응답에 실어 보낸다(`V1_VERIFICATION_DEV_ECHO`). 발송 수단이
하나도 없어 `issueChallenge` 가 503 을 내고 신규 가입이 막혀 있던 것을 푼다. 발송 자격증명은
오버레이에서 빈 값으로 눌러, 호스트 상태와 무관하게 우회가 꺼지지 않게 한다.
