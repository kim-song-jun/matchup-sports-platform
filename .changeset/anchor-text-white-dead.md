---
"v1_web": patch
---

파란 버튼 모양의 **링크** 다섯 곳에서 글자가 흰색이 아니라 어두운 색으로 나오고 있었습니다 — 어드민의 `대회 만들기`·`리그 만들기`, 권한 안내 화면의 `서비스로 돌아가기` 등입니다. 코드에는 흰 글씨로 적혀 있었지만 링크(`<a>`)에서는 그 지정이 먹지 않는 구조라, **코드를 읽어서는 보이지 않고 화면에서만 드러나는** 종류였습니다. 링크에서 확실히 먹는 방식으로 바꿨고, 같은 실수가 다시 들어오면 테스트가 막습니다.
