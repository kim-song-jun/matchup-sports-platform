---
"v1_api": patch
"v1_web": patch
---

배포가 실패했을 때 직전 릴리스로 되돌리는 복구 경로(alpha·prod)가 실제로 동작하도록 고쳤다. bash 는 ERR trap 핸들러 안에서 인자 없는 `return` 에 trap 을 일으킨 종료코드를 돌려주므로, 복구는 도입 이래 한 번도 성공한 적이 없었다(2026-09-08 alpha 복구 5회 연속 실패의 원인). 복구를 실제 trap 문맥에서 끝까지 돌리는 회귀 테스트를 CI 에 추가했다.
