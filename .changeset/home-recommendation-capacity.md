---
"v1_api": patch
"v1_web": patch
---

홈 "추천 매치" 카드의 참가 인원이 실제 값과 다르게 보이던 버그를 고칩니다. `GET /api/v1/home`의 추천 목록 항목이 참가 인원(`participantCount`)과 정원(`capacity`)을 빼고 내려주는 바람에, 프론트가 값을 지어내 모든 카드가 "0/1명 · 마감 임박"으로 표시됐습니다(실제로는 1/6명인 매치도 마찬가지였습니다). 서버가 두 값을 함께 내려주도록 고치고, 프론트는 인원을 모를 때 숫자를 지어내는 대신 인원 줄과 "마감 임박" 배지를 그리지 않습니다.
