---
'v1_web': patch
---

정규 리그 경기 카드를 대회 카드와 **같은 껍데기** 위에 만든다. 배치·간격·정렬 축은 공유하고
상태·결과 어휘는 각 축이 자기 단일 소스를 쓴다 — 두 축의 `status` 는 이름도 타입도 같은데
값 영역이 다르다. 통합 순위표는 대회의 `registrationId` 와 리그의 `teamId` 를 모두 읽는다.
