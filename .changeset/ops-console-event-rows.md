---
"v1_web": patch
---

Make the live game console readable at a glance: recorded events carry a team-colored rail plus an event-type dot instead of an unbroken grey list, the transmission section only appears while something is still queued or failed, and the header score is set large enough to read from across a table. The goal toast no longer collapses to one character per line on a 390px phone, and the assist sheet's bottom fade only appears when the list actually scrolls.
