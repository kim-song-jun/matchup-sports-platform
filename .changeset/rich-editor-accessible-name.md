---
"v1_web": patch
---

공지·팝업을 쓰는 **글 편집 칸에 이름이 없었어요.** 화면을 눈으로 보지 않는 분이 그 칸으로 이동하면 "편집 텍스트" 라고만 들리고, 무엇을 쓰는 칸인지 알 수 없었습니다. 바로 위에 "본문" 이라는 글자가 있는데도 그게 그 칸의 이름이라고 연결돼 있지 않았거든요.

굵게·기울임 같은 서식 버튼 18개는 전부 이름이 붙어 있었는데, 정작 글을 쓰는 칸만 빠져 있었습니다. 이제 "본문" 이 그 칸의 이름이 됩니다. 서식 도구 모음 이름도 함께 따라가도록 고쳤어요(공지 화면에서 "공지 내용" 처럼 다른 이름을 쓰면 도구 모음도 같이 바뀝니다).

보이는 화면은 달라지지 않습니다.
