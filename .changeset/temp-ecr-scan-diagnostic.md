---
"v1_api": patch
"v1_web": patch
---

Temporary diagnostic step to inspect ECR repository/registry scan configuration while investigating why image scans never register. To be removed once root cause is confirmed.
