---
'v1_web': patch
---

Require the depositor name to be typed when applying to a tournament. The wizard used to prefill it with the selected team's name, so the submit button turned active before the applicant entered anything — the application then carried a team name while the actual bank transfer arrived under a person's name, which is exactly what delays payment matching.
