---
"v1_web": patch
---

**어드민 목록의 잘못된 링크가 화면을 통째로 깨뜨리지 않습니다.**

주소창의 `?status=` 값이 오타이거나 예전 값이면 회원·매치·팀매치 목록이 **"불러오지 못했어요" 에러 화면**이 됐습니다. "다시 시도"를 눌러도 같은 값으로 다시 요청해 계속 실패했고, 필터를 직접 눌러 주소를 바꿔야 한다는 걸 스스로 알아내야 했습니다.

이제 알 수 없는 값은 조용히 **"전체"** 로 넘어갑니다. 문의 목록에만 있던 방어를 나머지 화면에도 같이 적용했습니다.
