---
"v1_api": patch
---

Refresh the production API image's Alpine OpenSSL runtime libraries to the patched repository versions so critical image findings continue to block vulnerable releases without preventing safe deployments.
