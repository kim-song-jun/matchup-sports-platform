---
"v1_web": patch
---

관리자 목록의 로딩 자리표시자 높이를 실제 행과 맞췄어요. 지금까지는 위아래 여백을 임의 값(14px)으로 잡아 높이를 흉내내고 있었는데, 행 높이(64px)를 직접 지정하도록 바꿔서 **로딩이 끝나며 목록이 살짝 튀는 일이 줄어듭니다.**

함께, 간격 검사 도구가 화면 코드(TSX)의 임의 값도 보도록 넓혔습니다. 지금까지는 스타일시트만 검사해서 `py-[14px]` 같은 표기가 그대로 통과했어요. 화면에 보이는 변화는 없지만, 앞으로 간격이 조용히 어긋나는 걸 막아줍니다.
