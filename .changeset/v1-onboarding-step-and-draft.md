---
"v1_web": patch
---

**온보딩 진행 상태가 실제로 한 만큼만 기록되고, 초안이 계정을 넘어가지 않습니다.**

실력을 고르고 "지역 선택하기"를 누르는 순간, 지역 화면을 보지도 않았는데 계정 상태가 **"지역 입력 완료"** 로 저장됐습니다. 여기서 앱을 닫으면 지역을 하나도 고르지 않은 채 완료로 남아, 운영자가 회원 정보를 볼 때 사실과 다른 상태를 보게 됩니다.

또 온보딩 중 고른 값(임시 저장)이 **계정 구분 없이** 저장돼, 같은 기기·같은 탭에서 다른 계정으로 들어오면 앞사람이 고른 종목·지역이 그대로 복원됐습니다. 본인이 고르지 않은 값으로 매칭 추천을 받게 되는데 화면에서는 자기가 고른 것처럼 보였습니다. 이제 계정별로 따로 보관하고, 구분이 없던 옛 임시 저장은 지웁니다.
