/**
 * 경기 기록 공개 동의의 정책 해시 단일 소스.
 *
 * `PUT /me/record-consent` 는 `policyHash` 를 함께 저장해 "무슨 문구에 동의했는지"를
 * 남긴다. 동의를 켤 수 있는 자리가 설정 화면 하나였을 땐 그 파일의 지역 상수로 충분했지만,
 * Task 154 로 홈 넛지 배너가 같은 API 를 호출하게 되면서 두 곳이 서로 다른 해시를 보낼 수
 * 있는 상태가 됐다 -- 그러면 같은 동의가 어디서 켰느냐에 따라 다른 문구에 동의한 것으로
 * 기록된다. 값을 여기 하나로 모아 그 드리프트를 구조적으로 막는다.
 *
 * **문구를 바꾸면 이 값도 함께 올린다.** 해시가 그대로면 옛 문구에 동의한 사용자와
 * 새 문구에 동의한 사용자가 기록상 구분되지 않는다.
 */
export const RECORD_CONSENT_POLICY_HASH = 'v1-public-record-consent-1';
