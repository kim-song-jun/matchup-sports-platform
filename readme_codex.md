# Codex 세션 이어받기

2026-09-15 KST 갱신(2차, 42흐름 재검증 완료 반영). 이 파일은 **읽기 순서와 현재 위치를 안내하는 입구**다. 상세 진행·결정·검증 기록의 정본은 Task 168 하나로 유지한다.

## 먼저 읽을 문서

1. [Task 168 — 전체 진행 정본](.github/tasks/168-competition-phase3-full-flow-verification.md): 최상단 최신 판정·재개 커서부터 읽는다. Phase 3 구현, PR/Alpha 배포, 실패 원인, 후보 파일, 실제 검증과 미완 항목이 모여 있다. 아래쪽의 과거 '진행 중' 문구보다 최신 기록을 우선한다.
2. [시나리오 허브](docs/scenarios/index.md): 관리자·팀장·팀원·첫 사용자·경기 운영자의 사용자 흐름과 QA 문서 연결. **2026-09-15 M11 마이그레이션+alpha 데이터 전체 재생성 이전 상태를 기준으로 한 옛 "41/42" 기록이라 그 자체로는 최신이 아니다** — 최신 판정은 Task 168 문서를 우선한다.
3. [Task 165 — 피리어드·인라인 결과 정정](.github/tasks/165-ops-console-league-inline-correction-periods.md), [Task 166 — 결과·기록 흐름](.github/tasks/166-result-flow-simplify-dispute-removal-records.md): 계약 참고용, 오늘 세션에서 변경 없음.
4. [저장소 지침](AGENTS.md), [.codex 진입 규칙](.codex/AGENTS.md), [QA 규칙](.codex/qa-rules.md), [프론트엔드 규칙](.codex/frontend-rules.md), [디자인 규칙](.codex/design-rules.md): 작업 전 확인.

## 현재 확인한 상태 — 2026-09-15 세션 완료분

- **기준선 UI 결함 3건 전부 dev 머지 + alpha 실측 재검증 완료.** M-A(수상자 프로필 링크)+M-M(팀 일정 상대팀 정보) = PR #1209, M-T(팀 명단 수정 권한) = PR #1210. 세 건 모두 alpha 실화면에서 권한별 대비까지 확인(멤버는 수정 버튼 0개, owner는 정상 노출 등).
- **사용자 제보 버그 신규 발견·수정·머지: 대회판 로스터→경기 참가자 동기화 결함.** 대회는 대진 생성 시점에 신청 명단을 경기 참가자로 한 번만 복사하고 로스터 잠금 여부를 안 봤다 — 로스터가 비었을 때 대진부터 생기면 참가자가 영원히 빈 채로 고정. 리그는 Task 170(D1′)이 이미 막았지만 대회는 범위 밖이었다. **[PR #1212](https://github.com/kim-song-jun/matchup-sports-platform/pull/1212)**로 해결(`syncTournamentRosterLineups`). Copilot 리뷰 2라운드 — 1차에서 실결함(등번호만 바뀌어도 동기화 안 되던 것, 단순 비교식 문제가 아니라 전용 경로에 호출 자체가 누락) 지적받아 수정+회귀 테스트+mutation 검증, 2차 Approval recommended. 로컬 통합 스펙(`v1_migrate_check` 템플릿 DB, 마이그레이션 168개 전량 재생) 10/10 PASS.
- **42흐름 QA 재검증 — 사용자 확정 기준 완료(2026-09-15).**
  - M-T/M-M/M-A + A-M(관리자 결과정정→공식화) alpha 실측 4건 전부 PASS.
  - 나머지 38흐름은 2단계 diff 감사(1단계: 커밋 메시지만 → 27흐름 의혹 / 2단계: 실제 diff 직접 읽기 → 27개 유효 확정·10개 재검증 필요·1개(S-T) 불확실로 좁힘)로 좁힌 뒤, 그 11개를 4개 병렬 ego-browser 에이전트로 alpha 실클릭 재검증. 8개 완전 PASS(라이브 또는 코드 근거), 1개(S-L 대진 교체 reconcile-in-place)만 **사용자 지시로 보류**(alpha의 유일한 리그 21경기 전부를 건드려야 해서 고위험 판단, 코드 diff 근거로 충분하다고 확정).
  - 재검증 도중 우연히 발견한 "리뷰 제출 403" 은 직접 curl로 재현·원인 규명한 결과 **버그 아님**으로 확정 — 실제 에러는 `NOT_ACTUAL_PARTICIPANT`(정상적인 출전자 검증)였고 QA 에이전트가 `NOT_TEAM_MEMBER`로 오진했던 것. 코드 수정 없음.
  - 상세 근거·커밋 해시는 모두 Task 168 문서 2026-09-15 항목들에 있다.
- **메인 작업트리 FF 지연 문제 해소.** 이전에 105커밋+ 뒤처져 있던 것은 이 세션의 정상 PR 머지·동기화 사이클로 전부 해소됐다 — 현재 로컬 dev는 origin/dev와 항상 동기화 상태. 더 이상 별도 확인 불필요.
- **main 승격 PR #735는 그대로 유지, 여전히 머지 금지.** 오늘 dev에 추가된 PR #1209/#1210/#1212로 승격 대상 diff가 더 늘었다 — 다음에 #735를 다룰 세션은 본문 경고가 이 최신 상태를 반영하는지 다시 확인할 것.
- 벤치마크 P0③(GTM 플레이북)은 사용자가 "나중에 별도로" 보류. 재요청 전까지 손대지 않는다.

## PR 정리와 승격 원칙 (변경 없음)

- 모든 구현 통합 대상은 dev. 필요한 PR은 수정 → 독립 리뷰·GitHub CI → dev 머지 → Alpha 검증까지 진행한다.
- [#735 dev → main 승격 PR](https://github.com/kim-song-jun/matchup-sports-platform/pull/735)은 통합 승격 입구로 유지한다. 에이전트가 main에 머지하거나 프로덕션 승격을 실행하지 않는다. main에는 아직 Task 168 M1–M11(레거시 fixture DROP 포함)이 없고 프로덕션 배포는 plain `prisma migrate deploy`라서, 승격하면 cutover·백업 없이 프로덕션 DB에 M1–M11이 한 번에 적용된다.

## 다음 세션에 바로 줄 지시

> readme_codex.md와 Task 168 최신 판정을 읽고 시작해줘. 42흐름 재검증은 2026-09-15에 사용자 확정으로 완료됐다 — 다시 재검증하지 마. 열려 있는 항목은 이 세 가지뿐이다.
> 1. **S-L 대진 교체(reconcile-in-place) 실경로 검증**: 코드 diff로는 안전 확인됐지만 alpha 실클릭은 사용자 지시로 보류 중이다. 사용자가 요청하면 alpha의 유일한 리그(21경기)를 대상으로 진행하되, 그 전에 반드시 alpha 데이터 쓰기 승인을 별도로 받아라.
> 2. **벤치마크 P0③(GTM 플레이북)**: 사용자가 "나중에 별도로" 하자고 명시적으로 보류했다. 사용자가 다시 꺼내기 전까지 자율로 시작하지 마라.
> 3. **#735 프로덕션 승격 PR**: 여전히 사용자 전용, 머지 금지. 본문 경고가 오늘 머지된 PR #1209/#1210/#1212를 반영하는지 확인하고 필요하면 갱신만 해라(머지는 하지 마라).
> 그 외 새로 사용자가 제기하는 버그·요청이 있으면 이 세션에서 확립된 패턴(worktree → 수정 → 로컬 통합 테스트 → Copilot 리뷰 루프 → dev 머지 → alpha 확인 → Task 168 문서 기록)을 그대로 따라라.
